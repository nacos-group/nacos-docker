# spring
server.servlet.contextPath=${SERVER_SERVLET_CONTEXTPATH:/nacos}
server.port=${SERVER_PORT:8848}

db.num=2
db.url.0=jdbc:mysql://${DB_HOST_ZERO}:${DB_PORT_ZERO:3306}/${DB_NAME_ZERO}?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true
db.url.1=jdbc:mysql://${DB_HOST_ONE}:${DB_PORT_ONE:3306}/${DB_NAME_ONE}?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true
db.user=${DB_USER}
db.password=${DB_PASSWORD}


